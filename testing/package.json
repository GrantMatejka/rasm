{
  "name": "testing",
  "version": "1.0.0",
  "description": "To run `node test.js <basename of file to test>` or `node test.js all` to run all tests",
  "main": "example_index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wabt": "^1.0.29"
  }
}
